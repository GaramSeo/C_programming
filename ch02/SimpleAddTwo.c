//SimpleAddTwo.c
#include <stdio.h>

int main (void) {
	int num1 = 3;
	int num2 = 4;
	int result = num1 + num2;

	printf ("the result of addition: %d\n", result);
	printf ("%d + %d = %d\n", num1, num2, result);
	printf("the addtion of %d with %d is %d.\n", num1, num2, result);
}